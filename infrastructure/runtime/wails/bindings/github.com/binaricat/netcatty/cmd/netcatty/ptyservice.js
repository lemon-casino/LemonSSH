// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * PTYService is the Wails-facing facade over the local PTY session owner
 * (P3-02). Sessions are addressed by opaque session IDs with generation
 * fencing; output is delivered to the renderer through the terminal data
 * plane (P3-01), not through JSON events.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

/**
 * Close terminates and reaps the session.
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function Close(sessionID) {
    return $Call.ByID(844764626, sessionID);
}

/**
 * Interrupt sends Ctrl+C into the PTY.
 * @param {string} sessionID
 * @param {number} generation
 * @returns {$CancellablePromise<void>}
 */
export function Interrupt(sessionID, generation) {
    return $Call.ByID(2728070617, sessionID, generation);
}

/**
 * Resize resizes the live PTY for the given generation.
 * @param {string} sessionID
 * @param {number} generation
 * @param {number} cols
 * @param {number} rows
 * @returns {$CancellablePromise<void>}
 */
export function Resize(sessionID, generation, cols, rows) {
    return $Call.ByID(375357898, sessionID, generation, cols, rows);
}

/**
 * Start launches a local PTY session and returns its generation.
 * @param {string} sessionID
 * @param {string} shell
 * @param {string} cwd
 * @param {string[]} args
 * @param {string[]} env
 * @param {number} cols
 * @param {number} rows
 * @returns {$CancellablePromise<number>}
 */
export function Start(sessionID, shell, cwd, args, env, cols, rows) {
    return $Call.ByID(1333065462, sessionID, shell, cwd, args, env, cols, rows);
}

/**
 * Write sends renderer input into the PTY.
 * @param {string} sessionID
 * @param {number} generation
 * @param {string} data
 * @returns {$CancellablePromise<number>}
 */
export function Write(sessionID, generation, data) {
    return $Call.ByID(2227434353, sessionID, generation, data);
}
